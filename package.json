{
  "name": "examgo-backend",
  "version": "1.0.0",
  "description": "Backend for ExamGo AI predictions — proxies requests to Google Gemini so the API key stays server-side only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.4.0"
  }
}
